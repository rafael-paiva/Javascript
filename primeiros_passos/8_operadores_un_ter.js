
/* Operador unário */
    var idade = 19;
    idade ++;        //Apenas uma operação

/* Operador ternário */
    var maiorIdade = (idade >= 18) ? true : false;   //se idade >= 18, maiorIdade = true, se não for maiorIdade = false
    console.log(maiorIdade);
